## What

Brief description of what this PR does and why.

## How

How it works, key decisions, or trade-offs made.

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] `npm test` passes (all 100 tests)
- [ ] No shell introduced into command execution (`shell: false` preserved)
- [ ] No approval enum values changed without maintainer discussion
- [ ] No secrets, keys, or credentials committed